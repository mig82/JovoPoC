const kony = require('kony-node')

module.exports = (jovo) => {
	kony.log("\n******************************** onResponse")
	//TODO: Push event to Fabric analytics.
	//kony.log(jovo.getResponseObject());
}
